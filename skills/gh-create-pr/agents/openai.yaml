interface:
  display_name: "GitHub PR Creator"
  short_description: "Ask first, then create Korean PR"
  default_prompt: "Use $gh-create-pr to first ask me for a PR title and PR description, turn them into a polished Korean pull request, assign it to me, and include a Closes #issue footer based on the current type-#issue branch."
